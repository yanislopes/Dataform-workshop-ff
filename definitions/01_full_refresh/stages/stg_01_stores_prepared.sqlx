config {
  type: "table",
  database: constants.PROJECT_ID,
  schema: "stage_french_fragrance",
  description: "Magasins nettoyés",
  tags: ["datamart"]
}

SELECT 
  store_id,
  TRIM(store_name) AS store_name,
  TRIM(city) AS city,
  TRIM(region) AS region,
  UPPER(TRIM(country)) AS country
FROM ${ref("raw_data", "stores")}